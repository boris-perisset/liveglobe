{
  "name": "globenews-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "textures": "node scripts/copy-textures.mjs",
    "predev": "npm run textures",
    "dev": "vite",
    "prebuild": "npm run textures",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "globe.gl": "^2.46.1"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
